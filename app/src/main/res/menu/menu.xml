<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_search_black_24dp"
        android:orderInCategory="100"
        android:title="@string/action_search"
        app:showAsAction="always"
        app:actionViewClass="androidx.appcompat.widget.SearchView" />
    <item android:id="@+id/sort"
        android:icon="@drawable/sort"
        android:title="Sort"
        app:showAsAction="always">
        <menu>
            <item android:id="@+id/newcase"
            android:title="New Case" />
            <item android:id="@+id/newdeath"
                android:title="New Death" />
            <item android:id="@+id/totalcase"
                android:title="Total Case" />
            <item android:id="@+id/totaldeath"
                android:title="Total Death" />
        </menu>
    </item>

</menu>